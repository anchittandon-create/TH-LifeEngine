export async function fetcher<T=any>(url:string, init?:RequestInit):Promise<T>{ const r=await fetch(url,init); if(!r.ok){ throw new Error(await r.text().catch(()=>`HTTP ${r.status}`)); } return r.json() as Promise<T>; }
